import "../app/globals.css";  // Import global CSS styles for the app

const LoadingSpinner = () => {
  return (
    <div className="spinner">
      {/* This is an empty div that will act as the loading spinner */}
    </div>
  );
};

export default LoadingSpinner;
